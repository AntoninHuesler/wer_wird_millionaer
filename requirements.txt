# Requirements.txt — Laufzeit-Abhängigkeiten
# Dieses Projekt verwendet ausschliesslich die Python-Standardbibliothek (tkinter, sqlite3, pathlib, random).
# Deshalb sind keine Pakete via pip notwendig.

# Hinweise:
# - SQLite ist integriert (sqlite3-Modul). Für CLI-Kommandos empfiehlt sich das Systempaket `sqlite3`.

black==24.8.0
flake8==7.1.1
pep8-naming==0.14.1